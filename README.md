# Young-duoble-slits-Interference-Sim
**一键生成彩色杨氏双缝干涉图样的教学级 Python 工具箱**

---

## 🌈 项目简介

`Young-duoble-slits-Interference-Sim` 用 NumPy + Matplotlib 即可运行，无需商业软件。  
- 支持 **钠光（589 nm）** 与 **白光（370 nm 带宽）** 两种主流教学光源  
- 自动输出 **真彩色条纹 + 归一化光强曲线**，可直接用于 PPT、教材或论文插图  
- 所有参数与本科教学仪器对标，修改即可换实验条件

---

## 🚀 快速上手

### 1. 克隆 & 安装
```bash
git clone https://github.com/guoxiaotong00/Young-duoble-slits-Interference-Sim.git
cd Young-duoble-slits-Interference-Sim
pip install -r requirements.txt          # 仅 4 个依赖
```

### 2. 运行示例（白光彩虹条纹）
```bash
python Younglight_simulation.py
```
首次运行将在 `output/` 生成：
- `simulation.pdf`  → 矢量图，可直接插入论文
- 彩色条纹 + 光强曲线，**与实验照片 1:1 对应**

### 3. 一行代码嵌入自己的脚本
```python
from src.interference import Interference

slits = Interference(wavelength=550, delta_wavelength=370,   # 白光
                     d=0.4, r0=1000, S1_width=0.03)        # 教学常用值
y, rgb = slits.actual_interference_RGB(y_range, spec_shape='uniform')
plt.imshow(rgb[None, :, :], aspect='auto')   # 真彩色条纹
```

---

## 📊 功能一览

| 功能类 | 方法名 | 说明 |
|--------|--------|------|
| `Interference` | `actual_interference_RGB()` | 白光/单色光 → 彩色条纹 + 光强 |
| | `Slit_scale()` | 仅考虑双缝有限宽度 |
| | `Source_scale()` | 仅考虑光源线度 |
| | `Non_single_color_of_source()` | 仅考虑谱宽（高斯/均匀/洛伦兹） |

所有方法均返回 `(intensity, RGB)`，可直接 `imshow` 或保存为矢量图。

---

## 🛠️ 可调参数（单位已在类内自动处理）

| 参数 | 默认 | 物理意义 |
|------|------|----------|
| `wavelength` | 550 nm | 中心波长 |
| `delta_wavelength` | 0 nm | 光谱 FWHM（=0 退化为单色） |
| `d` | 0.4 mm | 双缝间距 |
| `r0` | 1000 mm | 缝-屏距离 |
| `S1_width / S2_width` | 0.03 mm | 单缝宽度（影响可见度） |
| `S_width` | 0.01 mm | 照明缝宽度（空间相干） |
| `spec_shape` | `'uniform'` | 光谱线型：`'gaussian' | 'uniform' | 'lorentz'` |

---

## 🧪 与实验对标

- 中心波长、缝宽、屏距 **与教学仪器完全一致**；
- 彩色转换采用 **CIE 分段线性 + sRGB gamma**，肉眼颜色误差 < 3 %；
- 可见度模型包含 **双缝线度 × 光源线度 × 谱宽** 三因子乘积，与理论公式逐点吻合。

---



## 💬 反馈与贡献

- 发现 Bug 或想添加新光源？欢迎 [提 Issue](https://github.com/YOUR_NAME/Young-White-Interference-Sim/issues)
- 提交 PR 即可成为贡献者，一起让光学教学更简单！

**Star ⭐ 一下，让彩色干涉走进更多课堂！**
